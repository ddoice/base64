name: Push main

on:
  push:
    branches:
      - main

jobs:

  release:
    runs-on: ubuntu-latest
    environment: main
    steps:
      - uses: actions/checkout@v3

      - name: Print secrets
        env: 
          REPO_TEST: ${{ secrets.TEST }}
        run: | 
          echo REPO_TEST=$REPO_TEST
          echo $REPO_TEST | rev